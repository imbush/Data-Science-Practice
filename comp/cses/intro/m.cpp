#include <iostream>
#include <vector>

using namespace std;

int main() {
    int n;
    cin >> n;
    vector<string> binary = {"0", "1"};
    for (int i = 0; i < n; i++) {
        
    }
    for (int i = 0; i < gray.size(); i++) {
        cout << gray[i]
    }
}